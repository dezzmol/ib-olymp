import { EstimateTaskIdForm } from "@/modules/Estimate"
import classes from "@/styles/page.module.css"

const EstimateTaskIdPage = () => {
    return (
        <main className={classes.page}>
            <EstimateTaskIdForm />
        </main>
    )
}

export default EstimateTaskIdPage